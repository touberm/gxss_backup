$(function(){
  
  /* 最新活动滚动 */
 /*  $('#recent .con').height($('#recent .con ul').height()/2);
  var cur = 0;
  var recent_timer = setInterval(function(){
    console.log(cur);
    if((-cur) >= ($('#recent .con ul').height()/2) ){
      cur = 0
    }else{
      cur--;
    }
   
    $('#recent .con ul').css({'top':cur});
  },80); */

})